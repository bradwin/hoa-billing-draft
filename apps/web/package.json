{
  "name": "@hoa/web",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "npm run build --workspace @hoa/shared && next build",
    "dev": "next dev",
    "start": "next start",
    "test": "jest --config jest.config.js",
    "test:pbt": "jest --config jest.config.js --testPathPattern pbt --passWithNoTests"
  },
  "dependencies": {
    "@hoa/shared": "0.1.0"
  }
}
