describe('audit protected mutation integration skeleton', () => {
  it.todo('commits a protected settings mutation and required audit entry in the same transaction');
  it.todo('fails a security-sensitive mutation when audit append fails');
  it.todo('rejects runtime update/delete attempts against audit records');
});
