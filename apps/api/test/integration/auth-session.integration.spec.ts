describe('auth/session integration skeleton', () => {
  it.todo('logs in with valid credentials, creates server session digest, and returns a secure cookie through the HTTP layer');
  it.todo('revokes the server-side session on logout');
  it.todo('keeps missing account, inactive account, locked account, and wrong password responses non-enumerating');
});
